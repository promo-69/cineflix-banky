meta {
  name: Consultar Transaccion (quien recibe)
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/external/transactions/{{transactionReference}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{receiverToken}}
}

vars:pre-request {
  reference: {{transactionReference}}
}
